"def create_batch_summary_flex_bubble(uploaded_results, folder_name):\n    \"\"\"Creates a premium and highly-informative LINE Flex bubble for batch uploads.\"\"\"\n    total_val = 0.0\n    success_count = 0\n    duplicate_count = 0\n    items_contents = []\n    \n    for idx, r in enumerate(uploaded_results[:10]):  # Limit to 10 items to prevent Flex overflow\n        status = r.get(\"status\")\n        name = r.get(\"name\", \"ไฟล์\")\n        ext = r.get('analysis', {}).get('extracted_data', {}) if r.get('analysis') else {}\n        net_amt = ext.get('net_amount')\n        file_link = r.get(\"link\")\n        \n        parsed_amt = 0.0\n        if net_amt is not None and net_amt != \"-\":\n            try:\n                parsed_amt = float(str(net_amt).replace(\",\", \"\").strip())\n            except ValueError:\n                pass\n                \n        if status == \"success\":\n            success_count += 1\n            total_val += parsed_amt\n            amt_display = f\"{parsed_amt:,.2f} THB\" if parsed_amt > 0 else \"-\"\n            icon = \"🟢\"\n        else:\n            duplicate_count += 1\n            amt_display = \"⚠️ ซ้ำซ้อน\"\n            icon = \"🟡\"\n            \n        # File name text element with interactive Drive link if available\n        name_text = {\n            \"type\": \"text\",\n            \"text\": f\"{icon} {idx+1}. {name[:24]}\",\n            \"size\": \"xs\",\n            \"flex\": 6,\n            \"wrap\": True\n        }\n        \n        if file_link and str(file_link).startswith(\"http\"):\n            name_text[\"action\"] = {\n                \"type\": \"uri\",\n                \"label\": \"ดูไฟล์ต้นฉบับ\",\n                \"uri\": str(file_link)\n            }\n            name_text[\"color\"] = \"#007BFF\"  # Beautiful blue color for clickable links\n            name_text[\"decoration\"] = \"underline\"\n        else:\n            name_text[\"color\"] = \"#334155\"\n            \n        items_co
<truncated 635 bytes>